"use client";
import {useEffect,useState} from "react";
export default function MediaPanel({postId,version,disabled,api,onChange}:{postId:string;version:number;disabled:boolean;api:(path:string,method?:string,body?:unknown)=>Promise<any>;onChange:(post:any)=>void}){
 const [media,setMedia]=useState<any[]>([]),[alt,setAlt]=useState(""),[role,setRole]=useState("hero"),[placement,setPlacement]=useState(1),[file,setFile]=useState<File|null>(null),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);
 useEffect(()=>{api("posts/"+postId+"/media").then(d=>setMedia(d.media)).catch(e=>setMessage(e.message));},[postId]);
 async function run(fn:()=>Promise<void>){setBusy(true);setMessage("");try{await fn();setMedia((await api("posts/"+postId+"/media")).media);}catch(e){setMessage(e instanceof Error?e.message:"Image action failed.");}finally{setBusy(false);}}
 return <section className="editor-report"><h3>Article images</h3><p>Upload an approved image or generate one new candidate. Review it and write accurate alternative text before selecting it. Never upload patient information.</p><div className="editor-fields"><label>Image placement<select value={role} onChange={e=>setRole(e.target.value)}><option value="hero">Hero</option><option value="inline">Inside the article</option></select></label>{role==="inline"&&<label>After section number<input type="number" min={1} max={30} value={placement} onChange={e=>setPlacement(Number(e.target.value))}/></label>}</div><label>Alternative text<input value={alt} onChange={e=>setAlt(e.target.value)} maxLength={250}/></label><label>Approved image file<input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><div className="editor-actions"><button disabled={busy||disabled||!file||alt.trim().length<5} onClick={()=>run(async()=>{const form=new FormData();form.set("postId",postId);form.set("role",role);form.set("alt",alt.trim());form.set("placement",String(placement));form.set("file",file!);const r=await fetch("/api/admin/blog-media",{method:"POST",body:form});const d=await r.json();if(!r.ok)throw Error(d.error);setMessage("Candidate uploaded. Review before selecting.");})}>Upload candidate</button><button className="secondary" disabled={busy||disabled||alt.trim().length<5} onClick={()=>run(async()=>{await api("posts/"+postId+"/media/generate","POST",{requestId:crypto.randomUUID(),role,alt:alt.trim(),placement});setMessage("New candidate ready for visual review.");})}>Generate one image</button><button className="secondary" disabled={busy} onClick={()=>run(async()=>{})}>Load image library</button></div>{message&&<p role="status">{message}</p>}{busy&&<p role="status">Preparing the image. This may take a few minutes.</p>}<div className="editor-fields">{media.map(m=><article key={m.id}><img src={m.url} alt={m.alt} style={{maxWidth:"100%",borderRadius:12}}/><p>{m.role} · {m.source}</p><label>Candidate alternative text<input value={m.alt} maxLength={250} onChange={e=>setMedia(items=>items.map(item=>item.id===m.id?{...item,alt:e.target.value}:item))}/></label><button className="secondary" disabled={busy||disabled||m.alt.trim().length<5} onClick={()=>run(async()=>{const d=await api("posts/"+postId+"/media/select","POST",{mediaId:m.id,version,alt:m.alt.trim()});onChange(d.post);setMessage("Image selected. The draft needs editorial review again.");})}>Use reviewed image</button></article>)}</div></section>
}
